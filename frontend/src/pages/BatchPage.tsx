/** Placeholder for the batch-upload flow (built by a later issue). */
export function BatchPage() {
  return (
    <div className="space-y-3">
      <h2 className="text-2xl font-bold tracking-tight">Batch upload</h2>
      <p className="max-w-2xl text-muted-foreground">
        Upload many label applications at once and review the results in a queue. This flow lands in
        a later iteration; the single-label verification is the core of the prototype.
      </p>
    </div>
  );
}
